# Local dev + CI tooling (not baked into component images)
ruff==0.6.9
pytest==8.3.3
pre-commit==3.8.0

# Data libs handy for notebooks / local iteration
pandas==2.2.3
pyarrow==17.0.0
requests==2.32.3
pandera==0.20.4
psycopg2-binary==2.9.9      # local DB access for the snapshot export

# Notebooks / EDA / modelling (Week 2 prototyping)
jupyterlab==4.2.5
ipykernel
matplotlib==3.9.2
seaborn==0.13.2
statsmodels==0.14.2         # autocorrelation plots for lag selection
scikit-learn==1.5.2
xgboost==2.1.1
mlflow==2.16.2              # same version as the tracking-server container
